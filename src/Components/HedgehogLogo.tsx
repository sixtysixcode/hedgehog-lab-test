import hedgehogLogo from "../images/hedgehog-lab-logo.svg";

const HedgehogLogo = () => {
  return (
    <div className="logo-container">
      <img src={hedgehogLogo} alt="Hedgehog Labs" />
    </div>
  );
};

export default HedgehogLogo;
